<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://w-ok-e.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://w-ok-e.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-22T09:43:28+00:00</updated><id>https://w-ok-e.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Branch Prediction: From CPUs to GPUs and TPUs</title><link href="https://w-ok-e.github.io/blog/2026/asics/" rel="alternate" type="text/html" title="Branch Prediction: From CPUs to GPUs and TPUs"/><published>2026-06-20T18:00:00+00:00</published><updated>2026-06-20T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2026/asics</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2026/asics/"><![CDATA[<p>How does branch prediction work on CPUS?</p> <p>So essentially, in an NVIDIA GPU, the smallest unit of execution - a warp (which is made up of 32 threads that are locked together - i.e. they execute the same instruction at the same time) is responsible for the high parallelization responsible that is often talked about. Basically in things like Matrx multiplication, every operation looks identical that is, you multiply one row of the first mat with the second mat’s column and add them up. So this multiply and add operation can be run on each thread BUT on different data. And being locked together, it means that these threads will execute the instructions in the same lockstep.</p> <p>But what happens when the instructions are not identical? Say for example, you have a conditional branch in your code, like an if-else statement. In this case, some threads might take the if branch, while others might take the else branch. And since all the threads in a warp must execute the same instruction at the same time, this would lead to a situation where some threads are idle while others are executing the branch. This is known as branch divergence, and it is a major cause of performance degradation in GPUs because they must execute the commands serially now since you can’t execute both if and else at the same time. This is handled in CPUs via branch predictors, which try to predict the outcome of the branch and execute the instructions speculatively.</p> <p>TPUs take it a level up - dedicating even a larger portion of the on-chip computations to operations like matrix multiplication. Google’s TPUs use a systolic array architecture, which is a grid of processing elements that are connected in a way that allows data to flow through the array in a highly parallel manner. The need for fetching and writing data are eliminated here, what TPUs do is pass the tensors/arrays on which matmul must be performed from one cell to the other directly without writing to disk saving massive read/write overhead.</p>]]></content><author><name></name></author><category term="Technical"/><category term="Hardware,"/><category term="GPU,"/><category term="TPU,"/><category term="Performance"/><summary type="html"><![CDATA[Exploring the evolution of branch prediction and parallel execution across CPU, GPU, and TPU architectures.]]></summary></entry><entry><title type="html">ULT: Unifying Teacher-Student RL with Transformers</title><link href="https://w-ok-e.github.io/blog/2026/ult/" rel="alternate" type="text/html" title="ULT: Unifying Teacher-Student RL with Transformers"/><published>2026-06-01T12:00:00+00:00</published><updated>2026-06-01T12:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2026/ult</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2026/ult/"><![CDATA[<h2 id="the-student-teacher-paradigm-in-rl">The Student-Teacher Paradigm in RL</h2> <p>The student-teacher way of training RL policies is based off of the fact that not all privileged information is present in the real world. In this setup:</p> <ul> <li>The <strong>Teacher model</strong> learns to roll out better trajectories based on the additional (privileged) info it sees.</li> <li>The <strong>Student model</strong> learns to imitate the teacher model using only the information available in the real world.</li> </ul> <h3 id="offline-vs-online-training">Offline vs. Online Training</h3> <p>This can be done either offline or online:</p> <ul> <li><strong>Offline Approach:</strong> Whatever the teacher policy learns is stored in a dataset. We then train the student model on that dataset, saving the pairs of inputs (data modalities) and outputs (actions). The input is filtered to only contain the data streams the student model will have access to in the real world.</li> <li><strong>Online Approach:</strong> The teacher and the student model are trained together, where the student model learns to imitate the teacher model in real-time.</li> </ul> <h3 id="the-distribution-shift-problem">The Distribution-Shift Problem</h3> <p>During online training, the student uses its own policy to generate new trajectories. This means the student can go “off-distribution” from the teacher policy, known as the <strong>distribution-shift problem</strong>. To fix this, researchers often use <strong>DAgger</strong>: the student policy generates trajectories, the teacher policy labels the data points, and then the student policy is retrained on this new augmented dataset.</p> <h2 id="replacing-split-policies-with-ult">Replacing Split Policies with ULT</h2> <p>In the ULT paper, the core idea is to replace the split policies (teacher and student) with a <strong>single unified transformer architecture</strong>.</p> <p>Transformers are meant to process sequential data and find relations between every component of the input. However, if kept as-is, we would fail the core idea where only the teacher model has access to privileged information.</p> <h3 id="causal-masking">Causal Masking</h3> <p>The brilliant idea here is to use <strong>causal masking</strong>. This allows the transformer to only focus on certain parts of the sequence while ignoring the rest, effectively controlling what “privileged” information is visible at what time during the student’s training.</p> <h3 id="curriculum-training">Curriculum Training</h3> <p>A simple yet effective idea often used is <strong>curriculum training</strong>. By gradually increasing the difficulty of the training, we can make the sim2real transfer more robust, primarily against varied terrain.</p> <h2 id="deep-dive-the-ult-architecture">Deep Dive: The ULT Architecture</h2> <p>The architecture in the ULT paper houses a plain transformer, but the data processing is key:</p> <ol> <li><strong>Tokenization:</strong> The input trajectory (pairs of action/states) is tokenized simply by concatenating the observation-state pairs and passing them through a simple MLP (outputting a 128-dim token embedding).</li> <li><strong>Separation of Info:</strong> Privileged observation is not mixed with proprioceptive information. They are encoded separately: <ul> <li><strong>Proprioceptive (encoded):</strong> $d$ dim $(H_0, H_1, H_2, H_3 \dots)$</li> <li><strong>Exteroceptive (encoder):</strong> $d$ dim $(H_e)$</li> </ul> </li> </ol> <h3 id="sequence-preparation">Sequence Preparation</h3> <p>The input sequence is prepared as: $[H_0, H_1, H_2, H_3 \dots, H_e]$.</p> <p>By placing the <strong>exteroceptive information at the end</strong>, the proprioceptive information (the student) does not get access to it yet. This sequence is passed through the transformer’s attention module. To prevent “future looking” and to keep the privileged info separate from the student tokens, <strong>causal masking</strong> is applied. This ensures proprioceptive observations don’t gain insights from future pairs or the exteroceptive observations.</p> <h3 id="domain-randomization">Domain Randomization</h3> <p>Similar to other papers, <strong>domain randomization</strong> is commonplace. External factors in simulation (friction, force on the Quadruped, etc.) are randomized before being fed to the policy.</p> <h2 id="predicting-actions">Predicting Actions</h2> <ul> <li><strong>Student Policy:</strong> To predict the action, we just use the $(t-1)$ output tokens from the transformer $(H_0, H_1, H_2 \dots H_{t-1})$.</li> <li><strong>Teacher Policy:</strong> The teacher policy has access to $H_e$. Due to causal masking, the attention mechanism incorporates all required info into the final privileged token $H_e$. Thus, for the teacher, action generation only depends on $H_e$.</li> </ul> <p>In both cases, an MLP takes the respective token output and predicts the next action.</p> <h2 id="training-losses-and-exploration">Training, Losses, and Exploration</h2> <h3 id="loss-functions">Loss Functions</h3> <p>The training involves multiple loss terms:</p> <ol> <li><strong>Trajectory Prediction Loss:</strong> Computed between the predicted trajectory and the actual expert trajectory.</li> <li><strong>Imitation Loss:</strong> Calculated between the current action (student) and the future action given by the teacher (expert).</li> </ol> <h3 id="mix-ratio-and-exploration">Mix-Ratio and Exploration</h3> <p>Since ULT trains both models together, it needs to decide which policy’s action to use at each step to ensure both are aptly explored.</p> <ul> <li>A <strong>“mix-ratio”</strong> decides whether the teacher or student policy predicts the next action token.</li> <li>In massively parallelized environments, this is implemented as a <strong>mask $M$</strong>, where each entry (0 to 1) decides the choice for that specific step.</li> </ul>]]></content><author><name></name></author><category term="Posts"/><category term="Reinforcement"/><category term="Learning,"/><category term="Transformers,"/><category term="Robotics"/><summary type="html"><![CDATA[A deep dive into the ULT paper and how it replaces split teacher-student policies with a single unified transformer architecture for RL.]]></summary></entry><entry><title type="html">Breaking down SREGym!</title><link href="https://w-ok-e.github.io/blog/2026/sregym-rfc2/" rel="alternate" type="text/html" title="Breaking down SREGym!"/><published>2026-05-29T18:00:00+00:00</published><updated>2026-05-29T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2026/sregym-rfc2</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2026/sregym-rfc2/"><![CDATA[<p>So our initial PR (which closely mimicked the Reddit PI Day Outage) was not novel enough, partly because I had missed a few existing issues that were created recently and also because I ended up framing the PI day outage itself in the wrong way. But we are back!! With another issue, had to dig a few places for these because the rate with which the candidates are proposing issues and patching them up has become breath-taking. Anyways, let’s get to it:</p> <p>So the problem stems from a issue that was opened in the official Kubernetes repoitory - <a href="https://github.com/kubernetes/website/issues/41896">#41896</a>, and which was subsequently patched in the PR - <a href="https://github.com/kubernetes/kubernetes/pull/117927">#117927</a>. The crux of the issue it this:</p> <p>Pods in Kubernetes are the smallest self-sustaining unit of kubernetes which can run an application, which we have already seen in the previous post. And a pod may contain one or more docker containers which it manages. Now, when a pod starts, it’s configuration specifies the container images it requires and the container then pulls those images to get the application running. And as you would know, docker containers can either be public or private. Public docker containers, do not need any authentication to be pulled, while private containers do - the term for the authentication keys is “image pull secrets”, and the issue <a href="https://github.com/kubernetes/website/issues/41896">#41896</a> pointed out that in earlier versions of kubernetes, if a any of these image-pull-secrets were missing, the pod would fail start BUT WITHOUT ANY WARNING or LOGS. And this was exactly that was patched in <a href="https://github.com/kubernetes/kubernetes/pull/117927">#117927</a>.</p> <p>So the challenge for the agent would be investigate the cluster, figure out that the pod is failing to start because of missing image pull secrets and is emitting ImagePullBackOff Errors - and then check the specific reasons for the cluster’s ImagePullBackOff Errors - which would be missing credentials, find those missing credentials and then patch the broken pods to ensure that the cluster is back up again safely and it works. But ofcourse there are many more intricate details to this, because we must simulate the entire thing in the SREGym environment for the agent to be able to work in, and this time we went on to tackle a pretty interesting challenge - trying to simulate the problem for local kind-based clusters + cloud based aws instances, so let’s get into it:</p> <h2 id="the-initial-setup">The initial setup</h2> <p>So to list out the things we must get done:</p> <ol> <li>Launch the hotel-reservation app, wait for it to be up.</li> <li>Now, as default, the application just pulls from public docker images available in docker hub and don’t really need a secret or an auth key.</li> <li>Sooo, we must take the public image and then somehow, make it private!!! There is an obvious way to do this - just pull from a private docker image, but that comes with the headache of having to maintain the secret and keeping track of auth changes.</li> <li>As highlighted above, we cannot just switch to using a private docker image, so instead, we pull the image and push it to a private docker registry that we create in our self-hosted cluster and assign it an auth key. Clever, eh?</li> <li>The entire inject_fault() pipeline involves deletion of the image pull secret, adding the public docker image to the private one and then pointing the pod to pull from the private docker image instead of the public one.</li> <li>A key caveat here is that the mitigation for this broken image pull secret involves reconstructing the image pull secret, but how does an agent reconstruct the secret if it has never seen it? Random guessing won’t cut it I think. So, we also leave breadcrumbs pointing at the credentials registry with enough information for the agent to reach and reconstruct the secret.</li> <li>Once the secret has been added to the namespace, the agent must force the the pod to restart to pull the image from the private docker registry, and the most straightforward way of doing it is to just delete the pod, and upon restart, it is forced to pull the image again with the udpated image secret.</li> <li>Another sort of a tangent that we would be taking later in this article would be about setting up this cluster on aws - which trust me was a pain.</li> </ol> <h2 id="defining-the-problem-and-simulating-it-in-sregym">Defining the problem and simulating it in SREGym</h2>]]></content><author><name></name></author><category term="Posts"/><category term="Software"/><category term="Testing,"/><category term="Debugging,"/><category term="Site"/><category term="Reliability"/><category term="Engineering"/><summary type="html"><![CDATA[Quick Dive into SREGym - the evaluation benchmark for agents deployed in SRE settings.]]></summary></entry><entry><title type="html">Breaking down SREGym!</title><link href="https://w-ok-e.github.io/blog/2026/sregym/" rel="alternate" type="text/html" title="Breaking down SREGym!"/><published>2026-05-29T18:00:00+00:00</published><updated>2026-05-29T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2026/sregym</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2026/sregym/"><![CDATA[<p>So a couple of days ago, I get an email from a professor at UIUC asking if I would be interested in working with him on a project related to SRE evaluation. Well, it was sort of an evaluation task for a bunch of candidates including me, which we had to pass if we were to work under him. This is the breakdown of this:</p> <h2 id="background">Background</h2> <p>SRE or Site Reliability Engineering is a discipline that aims at reliably detecting points of failure in a system - be it software or hardware, diagnosing and then mitigating them. Think of it like a very methodical way of carrying out a bugfix in large scale systems.</p> <p>Now ofcourse in development, you can’t just target real systems running off the shelf programs, so we need to create “simulated setups” or clusters so to say. So in order to do that, we use - yes you guessed it - Kubernetes. Kubernetes is what software engineers use to deploy, scale and manage their applications in a distributed manner - which can either be on the cloud or on your own machines. The key things to keep in mind here are the “nodes” and the “pods”.</p> <p><strong>Nodes</strong> are the virtual machines that run your application. <strong>Pods</strong> can be thought of the smallest self-sustaining unit of kubernetes which can run an application. To make things easier, you can even connect this to a life-pod that is so dear to sci-fi writers, each pod is supposed to have all the required resources to run a particular application, which means storage, network resources etc. And these “pods” so to say are designed to run as together as a logical unit.</p> <h2 id="example-is-better">Example is better?</h2> <p>Okay, so instead of absolutely boring the hell out of you by talking about all the theory here, I should better illustrate this using an example from the SRE benchmark tool itself. Let’s take a sample scenario which requires an SRE saving : Hotel Reservations Systems</p> <p>Okay, let’s be a bit more specific - Service Port Conflict in Hotel Reservation System. But as promised, I will breakdown everything. So in a framework or design such as Hotel Reservation System, a service is essentially a component or a part of the entire system that does a specific function. So for instance “Booking Service” is a service that handles the booking of rooms, “Luggage Service” handles any additonal “baggage” (not emotional dw) that you might be bringing, “Payment Service” handles the financial aspects etc. Now in order for the system to work as desired, each service must be able to listen in for API calls and requirements from other services and respond to them. And that is where the concept of “ports” comes in.</p> <p>You see, just like we humans don’t want strangers to just throw in letters through our bedroom or dining room window, but through a mailbox, services also only listen on specific addresses called “ports”. So for instance, “Booking Service” might be listening on port 8080, “Luggage Service” on port 8081 and so on. Sooo, as the title would have clicked by now, service port conflict is essentially the problem where two or more services are assigned the same port and just like how my neighbor can’t use my mailbox, two services can’t use the same port. That’s it, that is the problem.</p> <h3 id="stepping-through-sregym">Stepping through SREGym</h3> <ol> <li> <p><strong>Accessing the CLI:</strong> First we will drop into the cli of SREGym, oh and if you want to follow along, you can go checkout their readme for setup instructions, this article is more of a deepdive into ONLY the technical side of things.</p> <p>So we start the cli version of SREGym which allows us to directly start processes, analyse the issues, then check if our assesment is correct and then mitigate them.</p> </li> <li><strong>Starting the Task:</strong> When we start the <code class="language-plaintext highlighter-rouge">hotel_reservation</code> task here, here’s exactly what happens: <ul> <li>When you start a simulated problem, it first sees if there is anything leftover from a previous run, like maybe stray ghost processes from any previous problem ID that might have run, or containers etc. It cleans it up.</li> <li>Then comes the life-pod role, it starts the necessary services - <strong>Prometheus</strong>, which is essentially the tool for them to be able to log the various metrics and system information, and <strong>Jaeger</strong>, which tracks how requests are flowing through the system, requests essentially are the messages travelling from one place to the other, <strong>Loki</strong> is for log aggregation, but it is different from the logs prometheus gives us.</li> <li><strong>Prometheus</strong> is specifically meant for gathering metrics, while <strong>Loki</strong> is meant to gather the various other system logs. So while Prometheus will give you superficial information - like the metric is too low or the cpu usage is high, rather if you want to check what maybe caused the issue or how it happened, we gotta go for the logs generated by Loki.</li> <li>Then we have the <strong>OpenEBS</strong> which is a storage level system that is used to provide storage to the pods and more importantly, “persistent storage” i.e. storage that remains even if the pods are moved or deleted.</li> </ul> </li> <li> <p><strong>Diagnosing the Issue:</strong> Alright so that was all that happens when starting the cluster or in other words - when you are simulating the bug. Let’s talk about the steps to actually figure out what issue there is and how to tackle that. First up we will go about inspecting what pods/services are up to see what is failing, so we need to check what are the various pods related to hotel reservation that the task has started here and check their status. So out of all the pods that get listed one particular one - <code class="language-plaintext highlighter-rouge">recommendation-677bbf995f-r8fq</code> is still “pending” i.e. it failed to start - probably because of the port conflict we talked about earlier.</p> </li> <li> <p><strong>Investigating the Failure:</strong> OKay so once we have the system up and running - how do we actually go about diagnosing what is wrong and fixing it? You check logs - to round out the reason behind why the thing has not started. Now the interesting thing about this problem is that you will see nothing in the logs - which is what happens to most systems in the real world - they fail silently. And as a side-tip always keep in mind that a pending pod in kubdernetes will have no logs because it never ran.</p> <p>So first we do a scan of all the pods as stated above and if something is pending, we ask the cluster - “WHY” as follows:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-A</span>
</code></pre></div> </div> <p>This gives us a list of all the pods that are active. Now very intuitvely, as we said, the pods are essentially individual components of the system that are running the bits and pieces around. And this very first command gives us a pretty good idea of what could be going wrong, we get a STATUS update about all the pods as to which are running and which ones are not.</p> <p>Now once we get the status of each pod that exists, we know which one is the culprit, the ones with either <code class="language-plaintext highlighter-rouge">pending</code> or <code class="language-plaintext highlighter-rouge">stopped</code> etc. denote one or the other failure with the system. In the case, our output look something like this:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                                            READY   STATUS    RESTARTS   AGE
hotel-reservation-frontend-579c99867f-2584x   1/1     Running   0          19m
hotel-reservation-frontend-579c99867f-57j5j   1/1     Running   0          19m
hotel-reservation-frontend-579c99867f-6568x   0/1     Pending   0          19m
hotel-reservation-frontend-579c99867f-8699d   1/1     Running   0          19m
hotel-reservation-frontend-579c99867f-8z56z   1/1     Running   0          19m
hotel-reservation-frontend-579c99867f-9j57p   1/1     Running   0          19m
</code></pre></div> </div> <p>You’ll see one of the services is still pending and has not really started. Interestingly in SREGym if you are prudent enough and check the startup logs, the problem with this pod can be seen way early since it’s a manually introduced one - you will see the system saying that it injected a service port conflict. However, ofcourse, we are not cheating here, we need to wire the agent to properly identify and then mitigate the issue.</p> </li> </ol> <h3 id="the-oracle">The Oracle</h3> <p><strong>An Oracle</strong> is a term that is used to check whether the solution given by the agent is correct or not. As such there are different kinds of Oracles:</p> <ul> <li>As we have seen above there are multiple stages to the problem: starting from identification (problem exists) to diagnosis (why it exists) to mitigation (fixing it).</li> <li>There are oracles accordingly, out of which the <strong>diagnosis oracle</strong> is an LLM. We cannot expect the agent or human to tell us why something is failing in the same wording everytime (i.e. if the answer is “The port is stuck at 9100”, some agent may give a elaborated answer, another may give a short one).</li> <li>So an LLM can better judge whether the given answer is close to the actual one or not.</li> </ul> <h2 id="key-terminologies">Key Terminologies</h2> <ol> <li> <p><strong>DaemonSets:</strong> Controllers that run in the background and that are meant to ensure that a copy of a pod runs on each node in the cluster. For instance if we have a pod that contains a system for monitoring logs - now ofcourse monitoring is required on each node of the cluster and as such we would need a copy of that pod running on each node. This can be easily managed by a DaemonSet. But the downside here is that because it’s job is to ensure that there is a pod on every node, for scenarios where we create more and more nodes, the daemon may keep launching additional pods for each node which will lead to OOM errors.</p> </li> <li> <p><strong>Docker Containers - ECR:</strong> Elastic Container Registry - This is a managed container registry service provided by AWS. It is used to store, manage, and deploy Docker container images. In large companies and fast-paced environments, with every new build/test/added functionality, a new docker image gets built, so they might end up having say, 10000 or higher number of docker images every day. Keep in mind, that mostly each docker image is just an newer version of the last docker container - so you don’t actually need all of them and must keep cleaning them up in order to save space and costs. A way to manage docker containers is via Amazon Elastic Registry which keeps track of your docker images and manages their deployment along with storage and use. But ECR has very limited rules in how the docker images are managed.</p> </li> <li> <p><strong>Where’s my custom resource:</strong> Custom Resources in Kubernetes are services which allow you to handle newer tasks better - basically by defining your own thing. So for instance, to manage a database, instead of using the default services, you can define your own service for it and manage it your own way. And the advantage here is that you get all of kubernetes commands that you would otherwise have if you went with a default option. Now this all seems great especially given that you have all the kubectl commands at your disposal. So the way they went about this was that whenever they wanted to update a custom resource, they would change the status of the custom resource to pending by using <code class="language-plaintext highlighter-rouge">kubectl set status = Pending</code> - which would prompt the controller to have a look at the custom resource in an attempt to mitigate the pending resource tag. All was good and great until Kubernetes switched versions. Post v1.12, Kubernetes no longer allowed users to set fields of custom resources via kubectl. So now for an update a custom resource would never go into the pending state which meant that the controller would never look at it - and the update would not go through.</p> </li> <li> <p><strong>AutoScaling Catastrophe:</strong> Autoscaler removes/adds pods depending upon the resource utilization. So for instance, if the CPU utilization of a pod goes above a certain threshold, the autoscaler will add more pods to handle the load. But this can get trickier in cases where a Pod WILL use up more CPU - but only during startup, like basically in the container, no matter what pod you start for the service, it is bound to use up a 100 percent - as part of a warmup phase after which it settles down. Now what ends up happening here is that the autoscaler notices that the pod is under heavy cpu load, so it must increase the number of pods - and those pods start at 100% too - which starts a cycle of the autoscaler adding more and more pods - leading to an OOM error.</p> </li> <li> <p><strong>Health Checks:</strong> So Kubernetes works this way - As we have said - there are individual deployable units - pods ofc. and each pod has one or more containers inside it - the containers run various services essentially. Now there is something called as Health Checks in Kubernetes, which deploys various probes to check on the health of your deployed cluster. And these health checks are per-container. A few sample probes are the <strong>Readiness Probes</strong> and the <strong>Liveness Probes</strong>. The readiness probe checks the status of the container and if it has failed, it simply says do not send traffic to this container. The liveness probe checks the status of the container and if it has failed, it simply restarts the container. But now, the thing is that the health of a “pod” depends upon the health of each individual container - so if even one container fails, the health of the pod is considered failed. But this might not be desired when the failed container is not of high importance, something like sshd-agent.</p> </li> <li> <p><strong>AZ Balancing:</strong> Kubernetes scheduler is not very good at AZ balancing for deployments of services. AZ - Availability Zone balancing means that the pods are distributed evenly across different availability zones in a cluster. Now the kubernetes scheduler may sometimes end up distributing loads unevenly across different availability zones - which can lead to some nodes being overloaded while others are underutilized. However the good thing here is that unlike the other issues where you may have to patch the entire kubernetes library and use it, here you can simply replace the scheduler with another image i.e. the scheduler uses a docker image to run.</p> </li> <li> <p><strong>Calico:</strong> An open source networking and network-security solution for containers. It acts as the engine that allows Kubernetes clusters to talk to each other.</p> </li> <li> <p><strong>BGP (Border Gateway Protocol):</strong> A routing protocol used to exchange routing information across networks. In the context of Calico, BGP is used to advertise pod routes between nodes so that they can reach each other directly.</p> </li> <li> <p><strong>IP-in-IP Mode:</strong> Encapsulates pod traffic inside standard IP packets, which allows routing to happen even if the underlying network doesn’t support BGP routes.</p> </li> </ol> <h2 id="choosing-the-problem-to-target">Choosing the problem to target</h2> <p>Now, in Reddit pi-day outage, what had happened was that a kubernetes version upgrade had resulted in a label removal from a node - kubernetes uses node labels to determine where to schedule certain workloads. And in case of reddit, the issue was embarassingly simple, Calico was configured to run route reflectors on nodes with a particular label:master, and the kubernetes upgrade removed that label due to which no node could be identified to behave as route reflector which meant that none of the other pods got any information since they were not connected. For the uninitiated, route reflectors are essentially nodes that can direct network traffic to and between different nodes, and I think you would agree that for a social media site like reddit, the thousands of nodes running various features must be able to communicate to each other, but in the absence of the route reflectors, this communication broke down.</p> <p>And for the first issue, I am thinking we can do something similar as a SREGym evalation task, my plan specifically is: Every k8s cluster has a HPA - an Horizontal Pods AutoScaler, which basically increases or decreases the number of pods depending upon the load. So imagine you have a pod running and it is using up about 80 percent of the CPU, so HPA would increase the number of pods, to ensure the task gets distributed. Now, we can pick an app that already exists in the SREGym benchmark and simulate a situation where the HPA’s reference label breaks down and it is unable to scale up the number of pods again. All the pods run normally here, but any sort of increase in load would lead to the service failing, because the HPA is unable to do it’s job. Maybe not very novel, but it’s a start.</p>]]></content><author><name></name></author><category term="Posts"/><category term="Software"/><category term="Testing,"/><category term="Debugging,"/><category term="Site"/><category term="Reliability"/><category term="Engineering"/><summary type="html"><![CDATA[Quick Dive into SREGym - the evaluation benchmark for agents deployed in SRE settings.]]></summary></entry><entry><title type="html">Let’s Paint! Shall we?.</title><link href="https://w-ok-e.github.io/blog/2025/3dgs/" rel="alternate" type="text/html" title="Let’s Paint! Shall we?."/><published>2025-11-20T13:30:00+00:00</published><updated>2025-11-20T13:30:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/3dgs</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/3dgs/"><![CDATA[<p>Well, 2025 has been the year for 3DGS, with a lot of research coming out in this field. Especially it has been the year of transformer-styled architectures that are capable of accelerating one or the other aspect of the 3DGS pipeline. Specifically, SFM or Structure from Motion, which used to take hours via traditional pipelines like colmap and AliceVision, were sped up by nearly factor of 1000x. When I started writing this blog, it was meant to be a discussion on a rather simple way of removing floaters from 3DGS, but as I delved deeper into the topic of 3DGS itself, I realized there’s a lot to it that I never fully understood, so let’s make this blog all about that shall we.</p> <h2 id="gauss-to-the-rescueeverywhere">Gauss to the rescue(everywhere)</h2> <p>There are a few very influential people whom I absolutely love and admire. And most of them have stemmed out of the discoveries I make every now and then related to the field to math. And one of these people is ofcourse - Carl Friedrich Gauss. And the admiration has to do with the fact that personally, for me, whenever a field starts going down a whirlwind of complexities and intractable proofs, his work comes to the rescue in one way or the other. Let me just quickly highlight some of them:</p> <ul> <li>He gave a way of visualizing Complex Numbers as 2D vectors, made them much less “imaginary”</li> <li>Normal Distribution - this shows up everywhere, a simple elegant idea that even the most random selection of data is centred around a mean</li> <li>When you are banging your head against a wall trying to solve a system of equation - Gaussian Elimination to the rescue.</li> <li>And ofcourse, when you realize that rendering Radiance Fields via neural networks is eating up too much of your time, just model them using Gaussians - Gaussian Splatting.</li> </ul> <p>Not to discredit the man, but the last one is instrumental to us here. 3D Gaussian splatting - as the name suggests, it aims to model a scene as a collection of Gaussians, more specifically, a collection of anisotropic gaussians. The word anisotropic is important there, but we will break down both of these first:</p> <ol> <li>Representation of 3D Scene as a Gaussian - First off, what is a gaussian actually? The way people talk about it, it’s very easy to get come away with the intuition that a gaussian is actually a “blob” in 3d Space - well kinda, but the blob and all it’s properties are simply values that we get from the gaussian function: \(G(\mathbf{x}) = \exp\left(-(\mathbf{x} - \mu)^T \Sigma^{-1} (\mathbf{x} - \mu)\right)\) where $\mu$ is the mean of the distribution - can be thought of as the center or the region where most of the mass of the blob is concentrated, and $\Sigma$ is the covariance matrix - for starters, Covariance basically tells us how dependent two variables are on each other, and when done for each pair of values as in a Covariance matrix, we end up getting how “spread” the data is.</li> </ol> <p>Now being anisotropic is what’s important here, it basically means that the gaussian is not a sphere where there’s equal spread of influence and information in the three directions - but rather they can vary, the gaussian is now an ellipsoid and it can stretch more in one direction than the other. This is achieved via the Covariance Matrix. For a isotropic or spherical gaussian, the covariance matrix is simply a diagonal matrix with equal values on the diagonal hence it is equally spread along each of the three axes and this matrix just reflects the scaling operation across each axes. \(\Sigma = \begin{bmatrix} \sigma^2 &amp; 0 &amp; 0 \\ 0 &amp; \sigma^2 &amp; 0 \\ 0 &amp; 0 &amp; \sigma^2 \end{bmatrix}\) But for the anisotrpic gaussian, the covariance matrix can be decomposed as the product of a rotation matrix and a scaling matrix, i.e. $\Sigma = R S R^T$ where $R$ is a rotation matrix and $S$ is a diagonal matrix with the diagonal elements being the scaling factors along the principal axes(x,y,z) of the ellipsoid.</p> <p>To understand how this plays out - imagine a scene where you have a long stick, now if you were to model this stick as a gaussian, you would want the gaussian to be elongated along the direction of the stick, hence the scaling matrix would be such that it scales the gaussian along the direction of the stick, and the rotation matrix would be such that it rotates the gaussian to align with the direction of the stick. This would prove to be diffcult if we were to use isotropic gaussians to model the stick, as we would need a lot of gaussians to model the stick, and even then the stick would not be modeled as accurately as we would like.</p> <p>There is one more piece to the puzzle : colors. The Gaussian function we just described can only represent a single quantity, which is how much does a given Gaussian contribute to a given point in space. But we need to represent colors as well. If you think of it, there are a few ways in which this can be done:</p> <ol> <li>Fixed color assignments to each 3d point in space, where each gaussian is assigned a color, and the color of the gaussian is the color of the point in space that it is modeling. And as you’d expect this would be very “brittle” in the sense when you rotate the camera or move around the scene, the color of the gaussian would not change, and hence the scene would not look realistic.</li> <li>View dependent color representation - So based on the angle you are looking at the gaussian from, the color changes. This is achieved by using something called as “Spherical Harmonics” modeling the color as a function of the viewing direction, which is basically the vector pointing from the camera to the gaussian. So you ask if I am looking from this direction what color would this part of the gaussian be in, spherical harmonics answer that.</li> </ol> <p>Besides the above attributes, the gaussians in a 3DGS have a few other additional attributes like opacity, and scale. Opacity is basically the probability that a ray of light will be absorbed by the gaussian, and scale is basically the size of the gaussian. To understand what floaters are however, we need to understand the optimization process behind 3dgs: The question is, given that you are representing the scene as a collection of gaussians, how would you possibly go about making the gaussians represent the scene as accurately as possible? Well, the most straightforward way to do this is to optimize the parameters of the gaussians such that the rendered image is as close as possible to the ground truth image i.e. what are looking for is a function that tells us how a given set of gaussians represents a given image, and the function should be differentiable with respect to the parameters of the gaussians. So this process of rendering the image from the gaussians must be differentiable, let’s see how that is done.</p> <p>As we have seen, each 3D gaussian is defined as: \(G(\mathbf{x}) = \exp\left(-(\mathbf{x} - \mu)^T \Sigma^{-1} (\mathbf{x} - \mu)\right)\) So think about what steps could possibly be involved in projecting or rasterizing this 3D gaussian onto a 2D image plane. First and sometimes not the most obvious one is to transform the gaussian from world space to camera space. What is the world space and what is the camera space now?</p> <p>So the world space is basically where each point has 3d Coordinates akin to the actual 3d coordinates of the point in the real world. Sure we choose a certain point as reference, but the relative positions of the points are the same as in the real world. The camera space is the space where the camera is at the origin and the z-axis points in the direction of the camera’s view. This makes the following things easier:</p> <ol> <li>We know now that whatever is in front of the camera will have positive z-coordinates and the ones behind the camera can be easily ignored while the rasterization process.</li> <li>The math of actually projecting the gaussians is made much simpler, which we will see how.</li> </ol> <p>So essentially, we have a few gaussians in 3d space, and we want to project them onto a 2d image plane. Now remember that even though a “gaussian” is a blob in 3d space, it is nevertheless a function which we are querying at every point and estimating how much of that gaussian is present at that point, and because we can query the function for any value of x, it makes the gaussian representation continous. So, when projecting the gaussian onto a 2-d plane, insted of transforming every point, can we transform the function itself and obtain a function which we can then query at every 2d point to obtain the rendered image?</p> <p>So to put it in steps:</p> <ol> <li>Transform the gaussians from world space to camera space - remember this transformation makes the camera the origin of the scene and the depth axis is along the direction of the camera’s view.</li> <li>Project the gaussians onto the 2d image plane, here we don’t transform the function itself, but transform only the attributes of the gaussian onto 2d plane. So, essentially we just transformed the query function in 3D to a query function in 2d. Let us look at the projection steps in detail to understand how this process is differentiable, which essentially is what all the fuss is about. The covariance matrix is first transformed into the camera view from the world view: \(\Sigma_L = W \Sigma W^T\) where $W$ is the rotation matrix that transforms the world space to camera space, again this simply gives us the benefits that we have described above of the depth being nicely scaled. But remember how I said that this transformation also makes a lot of projection math easier?</li> </ol> <p>Well, so essentially projecting a 3d gaussian into 2d means obtaining an equivalent function in 2d which represents the same gaussian as seen from the camera, so we will just transform the mean $\mu$ to $\mu_L = W\mu$ and the covariance matrix $\Sigma_L$ to 2d space. The mean of any gaussian in 3d space is just a point(x,y,z) so projection can be done via simple projection equations: \(x' = f_x \frac{x}{z} + c_x, y' = f_y \frac{y}{z} + c_y\) where $z$ is the depth of the point, $f_x, f_y$ are the focal lengths of the camera and $c_x, c_y$ are the principal points of the camera. What is essential to note is that projection from 3d to 2d is mostly just the scaling operation i.e. division by $z$ and then a linear transformation.</p> <p>However, the covariance matrix is a bit more complicated. You cannot just divide the matrix by the depth $z$ and expect it to be the same as the 2d covariance matrix, because the covariance matrix is describing a lot more than just position - it is describing the spread of the gaussian in the three directions and also how it is oriented in 3d space. And here we pull off a trick or rather a mathematical superpower. You see, the projection function involves division by $z$ and when looked from a global perspective, this is essentially a non-linear operation, and it is hard to apply a non-linear operation to the covariance matrix. Because if the transformation were linear, we could leverage the following property of matrices: If $\Sigma$ is a covariance matrix of a random variable $X$, then the covariance matrix of $AX$ is $A\Sigma A^T$, where $A$ is a linear transformation applied to $X$. In our case $\Sigma$ is the covariance matrix of all the points in the 3d gaussian, so if we could express this projection operation(division by $z$) as a linear transformation, we could simply apply the above property to the covariance matrix and obtain the 2d covariance matrix.</p> <p>And would you know, that this transformation is given by the Jacobian matrix of the projection function? Bear with me on this, we will see how the jacobian came into play here. So essentially the covariance matrix is telling me how much the blob spreads in 3d space, and I want to know that if a blob is spread by $\Delta x$ in 3d, what is its spread in 2d i.e. $\Delta u$. From the projection relation above: \(\Delta u \approx \frac{\partial u}{\partial x} \Delta x = \left( \frac{f_x}{z} \right) \Delta x\) Then you think about, what matrix carries the partial derivatives of the projection function? Well, that is the Jacobian matrix of the projection function. \(J = \begin{bmatrix} \frac{\partial u}{\partial x} &amp; \frac{\partial u}{\partial y} &amp; \frac{\partial u}{\partial z} \\ \frac{\partial v}{\partial x} &amp; \frac{\partial v}{\partial y} &amp; \frac{\partial v}{\partial z} \end{bmatrix} = \begin{bmatrix} \frac{f_x}{z} &amp; 0 &amp; -\frac{f_x x}{z^2} \\ 0 &amp; \frac{f_y}{z} &amp; -\frac{f_y y}{z^2} \end{bmatrix}\)</p> <p>And there is a hidden trick that might go unnonticed if you are not careful. Technically the projection depth division needs to happen for all the points and all the values of $z$ and should happen globally, however a single gaussian is small and inside that very small space represented by the gaussian, when we look at the curve plotted by the depth values $z$, it nearly approximates a straight line, so in the above matrix, \(\frac{\partial u}{\partial x} = \frac{f_x}{z}\) where $z$ is the depth of ONLY the centre of the gaussian, which means that we have found the linear transformation that we were looking for, the Jacobian Matrix gives us the linear transformation that can help us translate the 3D Covariance matrix into a 2D Covariance matrix, and as discussed above, the 2D Covariance matrix is given by, we can use the amazing property in linear algebra to calculate the 2D Covariance Matrix as: \(\Sigma_{2D} = J \Sigma_{3D} J^T\)</p> <ol> <li>Pheww, that was a long one, a subpoint just turned into a entire blog. Anyways, so the transformed attributes of the 3D gaussian splat, help us approximate how each point is influenced by the 2d gaussian splat. But remember here that the for a single region in 2d space, it will not just be one gaussian contributing, but a host of gaussians, so we must also have a way ot estimating the order and the contribution of each gaussian to the final color of that pixel. Now the way this is done is rather interesting:</li> </ol> <p>So here, the algorithm uses a trick called tile-based rasterization. Essentially the idea is that we divide the entire region into tiles of certain dimensions say 16 x 16 units, and then check the contribution of each of the gaussian to that tile. This prevents us from having to evaluate each gaussian for each scene, instead we only evaluate the gaussians that land on that tile, then the gaussians are sorted based on their depth which is easy now thanks to the camera view translation that we did earlier. Interestingly, this sorting is not done for each tile in an isolated manner, rather it is done on the GPU via Radix Sort and on GPUs Radix Sort works like a charm for large numbers. And in our case, the numbers are obtained from the depth values of the gaussians and also the tile they are on. So each gaussian is sort of given a “key” which mathematically is basically: \(Key = (Tile\_ID \ll 32) \ | \ Depth_{int}\)</p> <p>To clear the jargon of binary operations there, we left shifted the tile ID by 32 bits and then performed a bitwise OR operation with the depth of the gaussian. So imaine a particular tile say 001 in binary, left shifting it by 32 bits will give us 001 followed by 32 zeroes, and then we OR it with the depth of the gaussian, which essentially means that we are appending the depth of the gaussian to the tile ID. And the GPU takes this ID for all the tiles, and sorts them all together. I will not be going into the details of Radix Sort here, but the key is that it works extremely fast for large set of numbers, check a tutorial out here - <a href="https://www.youtube.com/watch?v=6du1LrLbDpA">Radix Sort</a></p> <p>Once the gaussians are sorted by their depth and associated with their tiles, the GPU splats them or rather paints the picture as follows:</p> <ul> <li>Each tile is given a separate GPU Thread, which handles the painting part for that tile.</li> <li>Painting here just means being able to identify what is the final color and opacity of a particular pixel in that tile due to the collected gaussians, and it is done via the formula: \(C = \sum_{i \in N} c_i \alpha_i \prod_{j=1}^{i-1} (1 - \alpha_j)\) well hold up, I will break it down for ya: i. $c_i$ is the color of the i-th gaussian. ii. $\alpha_i$ is the opacity of the i-th gaussian. iii. $\prod_{j=1}^{i-1} (1 - \alpha_j)$ is the “Transmittance” that defines how much light is left over after passing through all the gaussians which are in front of the i-th gaussian.</li> </ul> <p>If you think about it, all of the above steps save a lot of time and computation on the system, first off, we each thread handles one tile so the process is extremely parallelized, and secondly, when calculating transmittance, there will be a certain number of gaussians post which the transmittance nears zero i.e. no light passes through hence the GPU does not even need to process these gaussians, unlike NeRFs where the MLP is queried for every point in space, even empty space everytime you want to render the scene from a particular viewpoint.</p> <p>That’s it, that is essentially the entirety of the “splatting” pipeline, now what remains essentially is comparing the rasterized image with the Ground Truth image and calculating the loss - which can then be used to update the parameters of the gaussians via backpropagation. Hmm..Actually I wanted to discuss floaters in this post, but I guess we will need to reserve it for a new one &gt;_&lt; !!</p>]]></content><author><name></name></author><category term="Posts"/><category term="Deep"/><category term="Learning,"/><category term="Research,"/><category term="3dGS,"/><category term="Gaussian"/><category term="Splatting."/><summary type="html"><![CDATA[A primer on 3DGS.]]></summary></entry><entry><title type="html">A simple and intuitive guide to using uv - an awesome tool from astral!</title><link href="https://w-ok-e.github.io/blog/2025/uv_tutorial/" rel="alternate" type="text/html" title="A simple and intuitive guide to using uv - an awesome tool from astral!"/><published>2025-10-26T18:00:00+00:00</published><updated>2025-10-26T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/uv_tutorial</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/uv_tutorial/"><![CDATA[<h2 id="step-1---setting-up-uv">Step 1 - Setting up uv</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-LsSf</span> https://astral.sh/uv/install.sh | sh
</code></pre></div></div> <h2 id="step-2---setting-up-a-simple-environment-in-uv-and-reproducing-it">Step 2 - Setting up a simple environment in uv and reproducing it</h2> <p>Clone the repo:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/W-OK-E/UWe.git
</code></pre></div></div> <p>This repo follows the following folder structure:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UWe
|---UWe is the Parent project with some deps 
|
<span class="nt">----chimera</span>
|   |
|   <span class="nt">-----It</span> has non-conflicting deps with the parent folder
|---hunter
|   |
|   <span class="nt">-----It</span><span class="s1">'s dependencies are conflicting with the parent folder
|---sapphire
|   |
|   -----It'</span>s dependencies are conflicting with the parent folder but not folder chimera
</code></pre></div></div> <p>Each folder is designed to demonstrate the effectiveness of uv in isloating and manageing environments at lightning speed. But first we will start with a simple environment creation. Initialize a uv project in the root of the repo</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init

</code></pre></div></div> <p>The command basically creates a .lock file and a .toml file which track the dependencies of your project.</p> <p><strong>Install the dependencies</strong> There are two ways in which this can be done</p> <ol> <li>Safe way <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv add <span class="nt">-r</span> base_requirements.txt
</code></pre></div> </div> <p>This command installs the dependencies in a .venv folder in the same directory, while also adding it to the .toml and .lock files so that we can keep track of the installed packages.</p> </li> <li>Installation with no tracking <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv pip <span class="nb">install</span> <span class="nt">-r</span> base_requirements.txt
</code></pre></div> </div> <p>The above command will only install the package in the .venv folder without adding it to the .toml and .lock files.</p> </li> </ol> <p>Now it might not make much sense but let’s test what each of those methods mean for your projects:</p> <h2 id="the-safe-way">The Safe Way</h2> <p>Once you have installed base_requirements.txt via the Safe Way, go ahead and remove the .venv in the root of your project:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-rf</span> .venv
</code></pre></div></div> <p>This removes all the installed packages. Now run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv run tests.py
</code></pre></div></div> <p>tests.py just imports a few packages to test the environment. What uv does now is, before running the script it checks the .lock and .toml files for what packages should be present in the environment, and automatically installs them without you having to explicitly mention it. Magic!! Which means that if you have installed the dependencies via the first method and someone else where to clone the repo, they won’t need to follow a tedious environment setup, they can just proceeed to run the relevant files and the packages are automatically installed.</p> <h2 id="the-direct-way">The Direct way</h2> <p>First remove all the config files by running:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-rf</span> .venv uv.lock pyproject.toml 
</code></pre></div></div> <p>Initialize the project</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init
</code></pre></div></div> <p>Now a crucial aspect of trying to run <code class="language-plaintext highlighter-rouge">uv pip install -r base_requirements.txt</code>, is that you need to initialize the project and create a .venv by running:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv venv
</code></pre></div></div> <ul> <li>If you donot run <code class="language-plaintext highlighter-rouge">uv init</code> uv will start using any environment it finds which will be disastrous</li> <li>If you donot run <code class="language-plaintext highlighter-rouge">uv venv</code> uv won’t know where to pip install the requirements.</li> </ul> <p>Once that is done, install deps via:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv pip <span class="nb">install</span> <span class="nt">-r</span> base_requirements.txt
</code></pre></div></div> <p>This time the packages won’t be tracked by .toml or .lock files so when you do:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-rf</span> .venv
</code></pre></div></div> <p>And run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv run tests.py
</code></pre></div></div> <p>You will get an error saying package not found, because uv doesn’t know what deps it needs to install.</p> <p>That completes everything about setting up a basic uv environment</p> <h2 id="managing-sub-projects">Managing Sub-projects</h2> <p>Now let’s see the following cases:</p> <ol> <li>If you have sub-projects inside your project that have non-conflicting dependencies with the parent project.</li> <li>If you have sub-projects with conflicting deps with the parent project</li> <li>If you have a sub-project which has conflicitng deps with th parent project but can work with the deps of some other sub-project.</li> </ol> <h3 id="case-1">Case-1</h3> <p>So this is the easiest scenario, the sub-folder chimera has deps which donot conflict with that of the parent folder, so we can just do:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>chimera
uv init <span class="c">#Optional</span>
uv add <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p>The deps for chimera donot conflict with that of the parent project so the packages will get installed to the <code class="language-plaintext highlighter-rouge">../.venv</code> folder and the packages will get added to the <code class="language-plaintext highlighter-rouge">../uv.lock</code> and <code class="language-plaintext highlighter-rouge">../pyproject.toml</code></p> <h3 id="case-2">Case-2</h3> <p>The deps in the hunter folder are incompatible with that of the parent project so if you:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>hunter
uv add <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p>You will get the following error:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  × No solution found when resolving dependencies:
  ╰─▶ Because <span class="nv">requests</span><span class="o">==</span>2.28.2 depends on urllib3&gt;<span class="o">=</span>1.21.1,&lt;1.27 and your project depends on <span class="nv">requests</span><span class="o">==</span>2.28.2, we can conclude that your
      project depends on urllib3&gt;<span class="o">=</span>1.21.1,&lt;1.27.
      And because your project depends on <span class="nv">urllib3</span><span class="o">==</span>2.2.0, we can conclude that your project<span class="s1">'s requirements are unsatisfiable.
  help: If you want to add the package regardless of the failed resolution, provide the `--frozen` flag to skip locking and syncing.
</span></code></pre></div></div> <p>Now the following things are <strong>not recommended</strong>:</p> <ol> <li>Doing a frozen install as the error suggests, it will be very hard to reproduce the environment later</li> <li>Running <code class="language-plaintext highlighter-rouge">uv pip install -r requirements.txt</code> - This will install the package to ../.venv without any syncing or locking, and will remove the parent folder’s package version. And everytime depending upon where you run a script from the package will swing between two versions because it will keep looking at the .toml that has the parent folder’s deps. And if you run the a script in hunter that requires <code class="language-plaintext highlighter-rouge">urllib3==2.2.0</code>, it will give you an error.</li> </ol> <p><strong>Recommended</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>hunter
uv init <span class="nt">--no-worskpace</span>
uv add <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p>This will start treating the <code class="language-plaintext highlighter-rouge">hunter/</code> folder as a separate project.**If you run <code class="language-plaintext highlighter-rouge">uv init</code> without the <code class="language-plaintext highlighter-rouge">--no-workspace</code>, it will start treating <code class="language-plaintext highlighter-rouge">hunter</code> as a member of the parent workspace and give the same errors as above. Things to take care in this setup:</p> <ol> <li>If you want to run some script in the <code class="language-plaintext highlighter-rouge">hunter/</code> folder you must run it from that folder because uv looks up the immediate environment setup. So it uses the closest interpreter is used. Which means int he folder structure: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UWe
|
.venv
tests.py
<span class="nt">----chimera</span>
|   |
|   <span class="nt">-----It</span> has some dependencies
|   hunter
|   |
|   .venv
|    tests.py
</code></pre></div> </div> <p>If you run tests.py in the hunter folder from the parent folder it uses the packages in the parent folder’s .venv. So to use hunter’s venv you must:</p> <ul> <li>either run it from the hunter folder</li> <li>or activate the hunter virtual environment via: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source </span>hunter/.venv/bin/activate
</code></pre></div> </div> <p>And when running scripts, you will need to pass the <code class="language-plaintext highlighter-rouge">--active</code> flag:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv run <span class="nt">--active</span> hunter/tests.py
</code></pre></div> </div> </li> </ul> </li> </ol> <h3 id="case-3">Case-3</h3> <p>The deps in the sapphire folder conflict with the parent folder but are compatible with the deps of hunter/ folder, so you can use hunter’s environment globally but you will need to pass the <code class="language-plaintext highlighter-rouge">active</code> flag everytime:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source </span>hunter/.venv/bin/activate
</code></pre></div></div> <p>Then whatever you run a script pass the <code class="language-plaintext highlighter-rouge">--active</code> flag as:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source </span>hunter/.venv/bin/activate
uv run <span class="nt">--active</span> sapphire/tests.py 
</code></pre></div></div> <hr/> <p>This covers almost all of the intricate cases you might face while using uv, please note that while uv can completely replace pip, conda is still better in some cases, especially when your project has non-pythonic dependencies. For more details please head over to the documentation, it’s super cool - https://docs.astral.sh/uv/ <strong>**</strong><strong>**</strong><strong>**</strong><strong>**</strong><strong>**</strong><strong>**</strong><strong>**</strong><strong>**</strong><em>**</em></p>]]></content><author><name></name></author><category term="Posts"/><category term="Computer"/><category term="Vision,"/><category term="OpenCV"/><summary type="html"><![CDATA[Quick Dive into Byte Pair Encoding tokenizers.]]></summary></entry><entry><title type="html">A quick overview of Byte Pair Encoding tokenizers!</title><link href="https://w-ok-e.github.io/blog/2025/bpe_short/" rel="alternate" type="text/html" title="A quick overview of Byte Pair Encoding tokenizers!"/><published>2025-10-20T18:00:00+00:00</published><updated>2025-10-20T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/bpe_short</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/bpe_short/"><![CDATA[<p>Tokenizers are algorithms that convert text into numbers for computers to understand, and today we will be convering one such tokenizer - BytePairEncoding tokenizers. These are the tokenizers used in LLMs like GPT - 2, llama-3 etc. Let us talk about the preliminaries first - “Bytes”!</p> <p>A bit is the smallest unit of information representing either 0 or 1. 8 bits make up one byte. Now what is the most naive way to sort of encode text? Just swap every character with a bit, or if we are talking “byte encoding” by a bit. Now even if you were to be a bit cautious and not have different encodings for the same words that come in a text, you would still end up having a mammoth numerical encoding of the given text input(Consider the fact that the english language alone has over a million unique words, so having a vocabulary of over a million words is well not the most optimal thing to do.)</p> <p>But okay you’d argue that with increasing compute power, this really shouldn’t be an issue, but if you look carefully under the hood the biggest downside of this approach is not with large chunks of text, but with small ones. Now if a paragraph with say 50 words needs 50 tokens to be encoded that is excessive. Ideally we would like to minimize this overhead for smaller chunks of text. So even though BPE Tokenizers do involve replacing text with numbers, they do so in a clever way:</p> <p>Consider the following chunk of text: <code class="language-plaintext highlighter-rouge">fishing at five to return by the fire.</code> Now If we were to tokenize the text with one token for every character, we would end up with 38 tokens, which is obscenely large for such a small piece of text. So instead of having a token for every possible character or every possible word, BPEs have a token for every possible subword, subwords that can in turn help you form complete words. Consider for instance the subword ‘th’, which alone can form the basis of many words such as ‘this’, ‘that’,’there’,’thought’,’theme’ etc. so your vast wordspace gets compressed into subwords that appear most frequently. Let’s take see how BPE tokenizer handles the above sentence <code class="language-plaintext highlighter-rouge">fishing at five to return by the fire</code>:</p> <ol> <li>Identify Frequent Pairs <ul> <li>In the given text,’fi’ appears thrice</li> </ul> </li> <li>Replace and Record <ul> <li>Replace ‘fi’ with a new token ID that is not already in use e.g. 256 Hence the new text is:<code class="language-plaintext highlighter-rouge">&lt;256&gt;shing at &lt;256&gt;ve to return by the &lt;256&gt;re</code> And simultaneously we also update the vocabulary: {0:..(some text),1…..256:’fi’}</li> </ul> </li> </ol> <p>We do several iterations of the above two steps and build up the vocablulary. So given a large enough corpus, we can have a vocabulary capable of tokenizing any arbitrary text. Detokenization or inverse tokenization is also pretty simple, we just apply the reverse of the encoding steps until we obtain the actual text.</p> <p>Okay but nothing is complete today unless we can see it working in code for ourselves. So we are gonna write a basic tokenizer which algorithmically lies along the lines of the BPE tokenizer used in GPT-2. So let’s build the intuition for that step by step:</p> <h3 id="identifying-frequent-pairs">Identifying frequent Pairs</h3> <p>The first step as discussed, was to identify the most frequent pairs which can be merged together, now if you have worked with text you will realize that doing this with text is cumbersome and hard and it would be a lot better if we had the strings in numeric form so given let’s take it as numbers shall we(For the Sake of simplicity outputs are preceeded by »&gt;): Given a text, say:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">text</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A bunch of text</span><span class="sh">"</span>
</code></pre></div></div> <p>We will obtain the corresponding byte value for each character, coz of course if it is byte-pair encoding, but the reason for choosing bytes for this purpose is not arbitrary, bytes being the lowest rungs of the representation ladder which means they can encode anything and they can be reconstructed perfectly. Unlike unicode characters which can be inconsistent sometimes, having single or multiple codepoints for the same character(e.g. é). So bytes being a storage unit become language agnostic which means that the tokenizer can effortlessly handle any language or any symbols. Alright on with the implementation:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">text_byte</span> <span class="o">=</span> <span class="nf">bytes</span><span class="p">(</span><span class="n">text</span><span class="p">,</span><span class="n">encoding</span><span class="o">=</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span>
<span class="c1">#or
</span><span class="n">text_byte</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">text_byte</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span><span class="sa">b</span><span class="sh">'</span><span class="s">A bunch of text</span><span class="sh">'</span>
</code></pre></div></div> <p>In terms of bytes:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">print</span><span class="p">(</span><span class="nf">list</span><span class="p">(</span><span class="n">text_byte</span><span class="p">))</span>
<span class="o">&gt;&gt;&gt;</span> <span class="p">[</span><span class="mi">65</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">98</span><span class="p">,</span> <span class="mi">117</span><span class="p">,</span> <span class="mi">110</span><span class="p">,</span> <span class="mi">99</span><span class="p">,</span> <span class="mi">104</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">111</span><span class="p">,</span> <span class="mi">102</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">116</span><span class="p">,</span> <span class="mi">101</span><span class="p">,</span> <span class="mi">120</span><span class="p">,</span> <span class="mi">116</span><span class="p">]</span>
</code></pre></div></div> <p>So each number here is the byte representation of each character in the text i.e. 65 - A, 32 - ‘ ‘ etc. Now in order to carry out the process of replacement of pairs, we need to first figure out what pairs occur together most frequently.</p> <p>For a deep dive, please visit - <a href="https://sebastianraschka.com/blog/2025/bpe-from-scratch.html">BPEs</a></p>]]></content><author><name></name></author><category term="Posts"/><category term="Computer"/><category term="Vision,"/><category term="OpenCV"/><summary type="html"><![CDATA[Quick Dive into Byte Pair Encoding tokenizers.]]></summary></entry><entry><title type="html">Delegation, Discomfort and Decisions!</title><link href="https://w-ok-e.github.io/blog/2025/onurown/" rel="alternate" type="text/html" title="Delegation, Discomfort and Decisions!"/><published>2025-09-07T18:00:00+00:00</published><updated>2025-09-07T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/onurown</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/onurown/"><![CDATA[<p>This blog is gonna be rather short, suprisingly short actually - not just because I am busy juggling a bunch of things in my life at the moment, but also because this is more so a self-reflection than a full fledged analysis of a research paper or the latest update on my Quadruped task.</p> <p>The other day I was in the classroom, when someone brought up the idea of participating in a hackathon. And it was a reputed one so I gave a serious thought to participating in it. But then a thought quickly raced through my head, the thought that the people who were participating with me weren’t all that good, and me being me would end up doing most of the work even if it meant missing basketball - a sport that I love so dearly.</p> <p>But hey, it’s not just cases like these, I manage a team of over 40 people where I also head multiple projects. As the work begins I put people on todo boards and assign tasks, but then as time goes on, I begin taking up most of the work and with time I have realized it is because of the following reasons:</p> <ol> <li>The delay caused in assigning tasks, correcting their errors, explaining the workflow etc.</li> <li>The discomfort in asking them to do a certain task on top the existing tasks that they have been assigned.</li> </ol> <p>Okay valid, sometimes delegating tasks can slow things down, but then what I need to remind myself of the following things:</p> <ol> <li>The initial steps are slow, but with time, the people whom you assign tasks also grow, and the work becomes easier</li> <li>You have a better team to work with on new people, and also more time to do things that are meaningful.</li> </ol> <p>Then comes the decisions part, and more than just decisions, this segment is related to having ideas of your own. More often than not, I find myself being swept by the barage of suggestions and ideas, with little to no room for those of my own. These have the following repurcussions:</p> <ol> <li>When questioned I would have nothing better to say than “Oh It sounded right”, instead of being able to give a rational explanation about why the decision was made.</li> <li>Sometimes this leads to obscene amount of time being wasted, because you keep switching from idea to another, because all of them sound fascinating/brilliant, compounded by the lack of thoughts of your own.</li> </ol> <p>Now, don’t get me wrong, obviously suggestions from your team mates are invalulable and important, but what I want to stress here is the importance of having thoughts that are your own, and learning the art of delegating tasks in a way that help you and your team grow and learn.</p>]]></content><author><name></name></author><category term="Posts"/><category term="notes"/><category term="to"/><category term="self."/><summary type="html"><![CDATA[It's so easy to get lost in othe's thoughts or even our own ideas, is there a way to take this up? And why should we worry about whether]]></summary></entry><entry><title type="html">Watch and Learn, forget the speech!</title><link href="https://w-ok-e.github.io/blog/2025/saycan/" rel="alternate" type="text/html" title="Watch and Learn, forget the speech!"/><published>2025-08-31T18:00:00+00:00</published><updated>2025-08-31T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/saycan</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/saycan/"><![CDATA[<p>This is gonna be another one of the papers that I came across when working on integrating LLMs with the Quadruped, and it was rather an interesting find. While the ideas were intuitively simple, the methodology was definitely doing something unique and out of the box.</p> <h2 id="i-have-a-soul-but-no-body">I have a soul, but no body….</h2> <p>Providing instructions can be a bit tricky sometimes, mostly when we are not fully aware of the state/condition of a person whom we are trying to instruct. You could ask me how to boil an egg, and I would give you the steps to it, but that would also require me to know the resources you are working with, and your immediate surroundings or else the recipe will fall short. This applies more appropriately to Large Language Models breaking down complext actions for Robots, since the LLM lack any physical connection to the real world, their suggestions on how to do a particular task may come as very generalized or may turn out to be impossible to carry out(e.g. “Asking a robot to turn the AC on, but the room only has fans”). So this paper approaches this as sort of embodying the LLMs through a robot, to provide a better seed context for the LLM to produce informed actions.</p> <p>But the way they have proposed this embodiment is also rather interesting - each action that the robot can perform is given a certain ‘affordability’ value which is nothing but the probability that given the current state, how likely will that action result in a progress with the task. This score at each step makes the LLM aware of the robot’s ability to perform a certain sub-action. Hence the title of the paper ‘Say-Can’ in which the ‘Say’ part actually breaks a high level task into a sequence of sub-actions that can be performed to the task’s completion, while the ‘Can’ part is a learned affordance function that dictates the possiblity of carrying out the actions at every step.</p> <h2 id="getting-the-preliminaries-right">Getting the preliminaries right</h2> <p>Let’s talk about the components involved here. The first obvious thing are the LLMs, and to scratch the surface of what LLMs do - they basically generate the next token in a sequence based on a bunch of probability scores, that they obtain via the ever popular attention mechanism, this forms the ‘say’ part of the the whole pipeline. The next step is to be able to rank actions based on the robot’s given current state, and the go-to answer for the same was reinforcement learning, that too of a special kind - something called as Temporal Difference(TD) reinforcement learning. Here, the things are similar to your run-of-the-mill Markov Decision Process in terms of the reward function, the difference is in how the rewards are calculated, in non TD-RL the rewards are given at the end of an episode, while in TD, we sort of guess the future rewards that we might get, add them to the current reward and then evaluate our actions. This seems the right choice given the fact that the current action heavily depends on future actions, for instance you won’t decide on going to the museum tomorrow, if it’s a public holiday. And if you are someone who has read the classic paper of Reinforcement learning models learning the expression of Overall, the equation looks something like:</p> <p>\(V(s_t) \leftarrow V(s_t) + \alpha \big[ r_{t+1} + \gamma V(s_{t+1}) - V(s_t) \big]\) Here, V() is the value function that represents how ‘good’ a particular state is, r is the reward of the current state, and $\gamma$ is the discount factor, which basically decides how much to weigh the future reward. And this happens at every step, unlike the Monte-Carlo method where:</p> <p>\(V(s_t) \leftarrow V(s_t) + \alpha \big[ G_t - V(s_t) \big]\) is calculated at the final step, where $ G_t $ is the cumulative sum of rewards after all the steps.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_ims/say_can/td_mc-480.webp 480w,/assets/img/post_ims/say_can/td_mc-800.webp 800w,/assets/img/post_ims/say_can/td_mc-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_ims/say_can/td_mc.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><a href="https://medium.com/data-science/a-comprehensive-overview-of-gaussian-splatting-e7d570081362">src1</a></p> <h3 id="the-problem-statement">The Problem Statement</h3> <p>Alright so we need the following things:</p> <ol> <li>Given a complete task description, we want the LLM to break it down into sub-actions.</li> <li>The sub-actions also have a few constraints : <ul> <li>The robot should be able to follow them</li> <li>The actions should lead to the successful completion of the task.</li> </ul> </li> </ol> <p>Now this is carried out in two steps:</p> <ol> <li>Every low level action that the robot can directly perform is given a textual label l. From the high level instruction, the LLM tries to generate a sequence of steps which are basically textual labels for the robot’s actions. The LLM also generates probability values for each textual label which dictates the validity of that action given the state <ul> <li>e.g. consider the task of getting a tool from the other end of the room, and the state of the bot being in the centre. Here, so the action of moving towards the tool would be given a higher probability by the LLM.</li> </ul> </li> <li>Regardless of how favorable a given action is given the current state, we also need to consider the fact that the choosing that particular action also leads to the task completion, which is dictated by a Bernoulli Random Variable.(A Bernoulli Variable is a yes or no random variable that tells us whether the task is completed or not)</li> </ol> <p>So given a task, the architecture SayCan combines the probabilities of skills being relevant to the task with the probability from a affordability function that dictates whether the skill will lead to the completion of the task, and a product</p> <h2 id="bringing-ideas-to-reality-robotics">Bringing ideas to <del>Reality</del> Robotics</h2> <p>Now, even though it’s the LLMs task to come up with do-able tasks for the robot. It must know what tasks can the agent actually do. So we provide the LLM with a bunch of actions, their language descriptions, a value function which calculates the ‘affordance’ of the action, because the model needs a place to start. Now comes the question of how we train the agent to perform these skills and obtaining the affordance of these skills.</p> <p>Two methods were empoyed for the same:</p> <ol> <li> <p>Imitation Learning - The agents are exposed to multiple instances of tasks being performed and learns effective low level policies from that, their performance is then evaluated by human actors which decides if the task was compelted successfully or not.</p> </li> <li> <p>Multi-Task Learning - A suite of tasks(Handling, manipulating objects, movement) are performed and the robot learns common functionalities across those tasks. So instead of training policies for single tasks, they train multi-task policies i.e. a policy that can solve more than one task(Jargon alert: A Policy is nothing but something that translates given real world inputs into an action)</p> </li> </ol> <p>So the crux of the matter is that the LLM breaks down the instructions into simpler sub-tasks. The LLM basically generates the language description of the sub-tasks, then they used a sentence encoder to generate an embedding for each sentence. These embeddings are then fed to the policy and the value function that jointly outputs what action would be best suited for that scenario.</p> <h2 id="training-the-low-level-skills">Training the Low-Level Skills</h2> <p>Just like any robotics problem we start with training the agents in the simulation, and the underlying Markov Decision Process consists of a reward function, as well as skill specifications that are used by the model. But in order to expand over this idea and train the agents at scale in real world and to learn a RL policy that adapts well to language they used <strong>MT-OPT</strong> in the <strong>Everybody Robots</strong> simulator using <strong>RetinaGAN</strong> sim-to-real transfer. The <strong>action space</strong> of the policies includes six-degrees of freedom of the end-effector pose as gripper open and close commands, x-y position and yaw orientation delta of the mobile base of the robot, and the <strong>terminate</strong> action.</p> <p>A wide variety of skills were chosen for the robotic arms to be trained on, most of which included the skills that would be posed in a kitchen environment.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_ims/say_can/skills-480.webp 480w,/assets/img/post_ims/say_can/skills-800.webp 800w,/assets/img/post_ims/say_can/skills-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_ims/say_can/skills.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><a href="https://arxiv.org/abs/2204.01691">source</a></p> <p>Another advantage that this methodoloy carries is the freedom of choice when in comes to the LLMs that we use for the various tasks. The architecture is very modular and while we can use a different LLM for breaking down the higher level instructions into lower ones(planning), we can use a different one for obtaining the embeddings to each sentence. SayCan propses a unique approach to mapping higher level tasks into actions that are feasible, viable and appropriate for a robotic agent given a particular state.</p>]]></content><author><name></name></author><category term="Posts"/><category term="Deep"/><category term="Learning,"/><category term="Research,"/><category term="LLMs,"/><category term="Robotics,"/><category term="Quadruped."/><summary type="html"><![CDATA[The best way to learn something is to imitate the actor.]]></summary></entry><entry><title type="html">Lights❌ Language✅ Camera…Action</title><link href="https://w-ok-e.github.io/blog/2025/quarv/" rel="alternate" type="text/html" title="Lights❌ Language✅ Camera…Action"/><published>2025-08-24T18:00:00+00:00</published><updated>2025-08-24T18:00:00+00:00</updated><id>https://w-ok-e.github.io/blog/2025/quarv</id><content type="html" xml:base="https://w-ok-e.github.io/blog/2025/quarv/"><![CDATA[<p>If you are here from the last one, you would be reeling from all the researchy stuff we navigated or rather skimmed through back there. But Hey, you never really get to understand ideas completely unless you do a deep dive. And that is what we are gonna do here, a full in-depth review of the Quar-VLA papers which we discussed in the last one. So welcome to another article where you lay back with your popcorn, and let me do the heavy lifting of breaking down the complex jargon for you.</p> <h2 id="well-its-not-that-dumb-to-be-honest">Well, it’s not that dumb to be honest</h2> <p>If you noticed one major criticism of the papers discussed in the last blog were that they were mapping high level commands to a bunch of small tasks and not really into low-level motor or sensor commands. This sort of leaves the headache of infering the low level actions from the high level commands to the action manager, which then also needs to be trained as to what exactly particular “words” or “commands” map to in the action space. This is partly addressed by the Quar-VLA paper, which deals directly in the Vision-Language-Action Space, and they have also developed a specific VLA model for the same along with a multi-task dataset. But first let us explore their methodology and approach in depth, shall we?</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_ims/Quar_VLA/vision_action-480.webp 480w,/assets/img/post_ims/Quar_VLA/vision_action-800.webp 800w,/assets/img/post_ims/Quar_VLA/vision_action-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_ims/Quar_VLA/vision_action.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><a href="https://www.ecva.net/papers/eccv_2024/papers_ECCV/papers/00808-supp.pdf">src1</a></p> <h3 id="vision-language-action-models">Vision Language Action Models</h3> <p>An important thing to undestand here that if we are planning to build a model capable of producing motor-level commands from Vision and Language, we also need to look after the frequency at which the commands need to be sent. Because in the case of a quadruped, the actions that we output can neither be too simplistic like the velocity commands given by the nav planners, or require a very high frequency similar to the motor level commands. Hence having a dataset that bridges this gap is also of prime importance and this is one of the things the authors have aimed at, and QUAR-VLA seems to be the first architecture that integrates Vision and Lanuage together to generate actions.</p> <p>To this end, we come to talk about <strong>Vision Language Action Models</strong> that integrate the visual input from the various sensors on the Quadruped along with the language commands. The end goal more or less is to train a conditional policy QUART that can interporet RGB Images, and high level commands. This policy takes RGB Images and the instructions as input and produces actions as output. Now in their paper specifically the authors aimed at a 11-dimensional space, and each action command looks something like:</p> \[\{ v_x, v_y, ω_z, θ_1, θ_2, θ_3, f, h_z, φ, s_y, h_z^{f} \}\] <p>Here, v<sub>x</sub>, v<sub>x</sub>, and ω<sub>x</sub> represent the velocities along the x-axis, y-axis, and z-axis respectively. θ<sub>x</sub>, θ<sub>x</sub>, and θ<sub>x</sub> indicate the gait pattern, f denotes the frequency, h<sub>x</sub> represents the height of the robot, φ denotes the pitch angle, s<sub>x</sub> corresponds to the foot width, h<sub>x</sub><sup>f</sup> represents the foot height, and t indicates the termination signal of the action.</p> <h4 id="generating-a-discrete-action-space">Generating a discrete action space</h4> <p>So an action policy is basically supposed to take in the images and the language input data and generate a bunch of action commands in the 11 dimensional action space. Now for each possible action(velocity, rotaion angle etc.) there is a continous range of values with a lower and upper bound which makes it very hard for the policy to reliably learn a set of actions given a particular input. Now this does not mean that we keep a set of discrete values, or else that will defeat the purporse of having a smooth action space as output. Discretization here means obtaining a set of bins for each possible action so that we can reliably say that okay “Velocity 5 m/s forward with rotation 30 degrees falls into this bin, choose a continous value from that. Effectively narrowing down the search space and “discretizing” our model’s outputs.</p> <ul> <li>Each action has a continous domain with a lower and a upper limit, this interval is divided into 256 bins of equal width = (range)/256</li> <li>And for any give target value, the corresponding index of the bin that it falls into is given by GIF(a - lower bound of the action space)/width of bins</li> </ul> <p>Alright, so given this action space, multiple sets of actions were tried each in a different domain - perception, navigation, manipulation etc. each with a varying level of difficulty on the task. Some involved simple object detection and identification tasks, while others involved more complex identiying, manupilating and moving objects from one place to the other, and an obvious trend was observed. For more complex tasks, we required larger number of episodes as compared to tasks that were relatively simpler.</p> <p>But what was more interesting was the insights on the relationship between the tasks that were assigned:</p> <ul> <li>Perception emerged as the a foudnational task, underpinning the other tasks</li> <li>Basic Navigation came next</li> <li>As an extension of the above two fundamental tasks, arose the four - Spatial Navigation, Environment Adaption, Obstacle Avoidance and Object Manipulation.</li> </ul> <p>The following picture from the paper does a nice job of illustrating the same:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_ims/Quar_VLA/action_rel-480.webp 480w,/assets/img/post_ims/Quar_VLA/action_rel-800.webp 800w,/assets/img/post_ims/Quar_VLA/action_rel-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_ims/Quar_VLA/action_rel.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><a href="https://arxiv.org/abs/2312.14457">src1</a></p> <h2 id="devil-in-the-details">Devil in the Details</h2> <p>I would like to list a few key aspects of the author’s approach, since I am working on mine as well, so if you have stuck around till this part of the blog, that’s terrific, let’s take note of a key constraint involved in the same:</p> <p><strong>Consistency Constraints</strong> - Sim to Real is one of the hardest problems out there in general. Forger about training a fancy quadruped. Transferring polcies from simulation to real life even for something as small as a robotic arm involves a lot of quirks, and the sheer amount of variables involved can be tumultous. Hence to maintain consitency between the simulation environment and the real world, the authors took a few key steps:</p> <ul> <li>The starting postion of the robot was always the origin, with the target randomly positioned between a set of fixed coordinates.</li> <li>The data collection setup adheres to a predefined language template where the task, the object, speed and the gait are pre-defined.</li> </ul> ]]></content><author><name></name></author><category term="Posts"/><category term="Deep"/><category term="Learning,"/><category term="Research,"/><category term="LLMs,"/><category term="Robotics,"/><category term="Quadruped,"/><category term="Vision"/><category term="Language"/><category term="Models."/><summary type="html"><![CDATA[Deep Dive into the Quar-VLA paper]]></summary></entry></feed>